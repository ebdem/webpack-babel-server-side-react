import React from "react";

const Home = () => {
    return (
        <div>
            <div>I am the Ebubekir Naber home component</div>
            <button onClick={() => console.log("Clicked")}>Click Me</button>
        </div>
    );
};

export default Home;